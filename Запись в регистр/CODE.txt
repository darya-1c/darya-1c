Код в модуле объекта «РемонтАвто» для списание материалов из регистра накопления «НаличиеЗапасныхЧастейИМатериалов»

Процедура ОбработкаПроведения(Отказ, Режим)
	
	Движения.НаличиеЗапасныхЧастейИМатериалов.Записывать = Истина;
	Для Каждого ТекСтрокаЗатраченныеМатериалы Из ЗатраченныеМатериалы Цикл
		Движение = Движения.НаличиеЗапасныхЧастейИМатериалов.Добавить();
		Движение.ВидДвижения = ВидДвиженияНакопления.Расход;
		Движение.Период = Дата;
		Движение.Материалы = ТекСтрокаЗатраченныеМатериалы.Материал;
		Движение.Количество = ТекСтрокаЗатраченныеМатериалы.Количество;
	КонецЦикла;
КонецПроцедуры



Код в модуле объекта «РемонтАвто» для отражения поступления денежных средств в регистр накопления «ДенежныеСредства»

Процедура ОбработкаПроведения(Отказ, Режим)
	Движения.ДенежныеСредства.Записывать = Истина; 
		Движение = Движения.ДенежныеСредства.Добавить();
		Движение.ВидДвижения = ВидДвиженияНакопления.Приход;
		Движение.Период = Дата;
		Движение.ИсточникПоступленияДенежныхСредств = Ссылка;
		Движение.ДенежныеСредства = ОбщаяСуммаДокумента;
КонецПроцедуры

		
L